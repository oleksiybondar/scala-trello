import type { ReactElement } from "react";

import { BoardMembersManagementCard } from "@components/boards/board-settings/members/BoardMembersManagementCard";

export const BoardMembersSettingsPage = (): ReactElement => {
  return <BoardMembersManagementCard />;
};
