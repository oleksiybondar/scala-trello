export interface DictionarySeverity {
  description: string | null;
  severityId: string;
  name: string;
}

export interface DictionaryTimeTrackingActivity {
  activityId: string;
  code: string;
  description: string | null;
  name: string;
}
