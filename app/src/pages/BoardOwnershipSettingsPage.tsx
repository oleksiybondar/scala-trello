import type { ReactElement } from "react";

import { BoardOwnershipManagementCard } from "@components/boards/board-settings/ownership/BoardOwnershipManagementCard";

export const BoardOwnershipSettingsPage = (): ReactElement => {
  return <BoardOwnershipManagementCard />;
};
